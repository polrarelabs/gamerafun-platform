import { QuestDetail } from "@components/screens/quest-detail";
import React, { memo } from "react";

const QuestDetailPage = () => {
  return <QuestDetail />;
};

export default memo(QuestDetailPage);
